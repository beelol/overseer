{
  "fixture": "atlas-tenant-isolation",
  "version": 1,
  "runtime": "Node.js 24, Express 5.2.1, PostgreSQL 16, in-process local object-store emulator",
  "jobs": ["J1-projects", "J2-tasks", "J3-membership", "J4-attachments", "J5-exports", "J6-tokens", "J7-independent-reproduction"],
  "seeded_findings": ["foreign-task-mutation", "foreign-attachment-download"],
  "protected_paths": ["project-membership", "role-escalation", "export-workspace-binding", "read-only-and-revoked-tokens"],
  "variants": {
    "task-guarded": "J7 only: add project-membership guard before task mutation to produce a contradictory independent result",
    "export-queue-missing": "J5 only: remove the local PostgreSQL export queue relation so own-workspace enqueue returns 503 and coverage stays blocked"
  },
  "isolation": "one PostgreSQL schema per scripted worker; every schema has independent seed rows"
}
