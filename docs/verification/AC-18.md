# AC-18 — Recursive run tree
Status: verified
Tested implementation commit: 8103a2e
Verification date and verifier: 2026-09-24/25, implementing agent (Claude Code)
OS / architecture / VS Code / harness versions: macOS 26.6.2 (25G83) arm64; VS Code 1.139.0 (stable, isolated --user-data-dir/--extensions-dir, VSIX installed with `code --install-extension`); Rust 1.89.0; Node 24.20.0; Git 2.50.1 (Apple); codex-cli 0.155.0-alpha.16.4 (ChatGPT.app bundle); Claude Code 2.1.246; OpenCode 1.15.13; overseerd 0.1.0 (protocol 1, parser 2026-09-24.1)
Harness, provider and redacted account identities (if applicable): not applicable (fixture harnesses; no accounts)
Prerequisites and fixture: Real Git repositories created per test under /tmp; isolated OVERSEER_HOME; isolated VS Code profile for UI scenarios

Steps or exact reproducible commands:

`cargo test` (daemon/tests/protocol.rs, real daemon binary + real Git fixtures): `ac18_fixture_recursive_tree_duplicates_and_delayed_parent` (root → child → grandchild, duplicated child event, grandchild reported before its parent; delayed parent adopts the provisional child; no cycles; identical after daemon restart; children share the parent workspace), `ac21_*` (independent tasks in separate workspaces). OpenCode mock run produced a real three-level tree in the packaged UI ([main](evidence/ui/main/), 'three-level native tree visible').

Expected result: Children and descendants with identity/status/harness/account/workspace; ≥3 levels; shared and separate workspaces; duplicates and delayed parents; no cycles after reconnect.

Actual result: All pass. Tree items show status, harness, account, workspace and relationship evidence/confidence.

Evidence paths (test logs, redacted transcripts, screenshots/recording): protocol tests; `evidence/ui/main/`
Live vs fixture coverage: Fixtures + OpenCode mock runtime.
Known limitations and remaining platform/account combinations: macOS only; Linux belongs to AC-41.
Blocker, attempted alternatives and next action (if blocked): not blocked
