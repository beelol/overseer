# AC-13 — Credential isolation on macOS
Status: blocked
Tested implementation commit: fdf1340
Verification date and verifier: 2026-09-24/25, implementing agent (Claude Code), overnight session
OS / architecture / VS Code / harness versions: macOS 26.6.2 (25G83) arm64; VS Code 1.139.0 (stable, isolated --user-data-dir/--extensions-dir, VSIX installed with `code --install-extension`); Rust 1.89.0; Node 24.20.0; Git 2.50.1 (Apple); codex-cli 0.155.0-alpha.16.4 (ChatGPT.app bundle); Claude Code 2.1.246; OpenCode 1.15.13; overseerd 0.1.0 (protocol 1, parser 2026-09-24.1)
Harness, provider and redacted account identities (if applicable): not applicable (fixture harnesses; no accounts)
Prerequisites and fixture: Real Git repositories created per test under /tmp; isolated OVERSEER_HOME; isolated VS Code profile for UI scenarios

Steps or exact reproducible commands:

Implemented: per-profile `CODEX_HOME`, `CLAUDE_CONFIG_DIR`, `XDG_*` homes (0700), no credential values stored by Overseer (only one-way fingerprints of account ids), system logins never logged out. Protocol test `ac13_isolated_profiles_have_separate_homes_and_no_keys`.

Expected result: Live A/B logout/login during B's work, restart both, refresh/expiry fault tests, no leakage.

Actual result: Not run: needs two signed-in dedicated test profiles. Observation relevant to isolation: the shared `~/.codex` login switched accounts underneath during the session (see AC-02), which is exactly why isolated profiles are the default recommendation.

Evidence paths (test logs, redacted transcripts, screenshots/recording): protocol test; AC-02
Live vs fixture coverage: None.
Known limitations and remaining platform/account combinations: macOS only; Linux belongs to AC-41.
Blocker, attempted alternatives and next action (if blocked): Owner-provided dedicated test logins (A and B). Next: A logout/login while B runs a `sleep` turn; restart; compare fingerprints; inspect SQLite/events for token leakage (`grep` for token patterns).
