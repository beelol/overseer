# AC-61 — Needs-you inbox and keyboard control
Status: verified
Tested implementation commit: 3f8c0f9
Verification date and verifier: 2026-09-26, implementing agent (Claude Code)
OS / architecture / VS Code / harness versions: macOS 26.6.2 (25G83) arm64; VS Code 1.139.0 (stable, isolated --user-data-dir/--extensions-dir, VSIX installed with `code --install-extension`); Rust 1.89.0; Node 24.20.0; Git 2.50.1 (Apple); codex-cli 0.155.0-alpha.16.4 (ChatGPT.app bundle); Claude Code 2.1.246; OpenCode 1.15.13; overseerd 0.1.0 (protocol 1, parser 2026-09-24.1)
Harness, provider and redacted account identities (if applicable): Claude fixture (two permission requests), generic runs (a failure, a finished edit, a long loop)
Prerequisites and fixture: Real Git repositories created per test under /tmp; isolated OVERSEER_HOME; isolated VS Code profile for UI scenarios

Steps or exact reproducible commands:

`node test/ui/scenario-keyboard.js`: four runs need the user; then, with the keyboard only, ⌥⌘J (next that needs you), ⌥⌘Y (allow), ⌥⌘J, ⌥⌘⌫ (deny), ⌥⌘J until Needs you is empty, ⌥⌘A (switch agent, searchable), ⌥⌘. (stop), ⌥⌘N (new agent) and Enter, a follow-up with Enter; then an accessible-name audit of every visible control.

Expected result: A scripted keyboard-only session over three or more concurrent runs answers permissions, reviews changes and sends follow-ups without a click; all controls have screen-reader labels.

Actual result: - **Needs you:** 2 × Approve, 1 × Failed, 1 × Review, badge 4; the status bar reads "Overseer 3 active" with a bell and 4.
- **Keyboard:** ⌥⌘J selected the first permission request; ⌥⌘Y allowed it (completed); ⌥⌘J moved to the second; ⌥⌘⌫ denied it (`permission_answered` allow=false). ⌥⌘J then visited the failure, the allowed run (now a Review, since it wrote a file) and the finished edit; Needs you emptied.
- **Switch and stop:** ⌥⌘A → "Long loop" → ⌥⌘. interrupted it.
- **New agent and follow-up:** ⌥⌘N focused the composer; typing and Enter started an agent that became selected; Enter in the chat sent a follow-up (2 turns).
- **Labels:** 57 controls checked, none without a name.
- **Fixed on the way:** ⌥⌘J now goes to the most urgent item that is not already open (it used to cycle past items).

Evidence paths (test logs, redacted transcripts, screenshots/recording): [keyboard scenario](evidence/ui/keyboard/)
Live vs fixture coverage: Fixture runs; permissions from live Claude and Codex app-server use the same path (AC-43).
Known limitations and remaining platform/account combinations: The follow-up step focuses the chat's prompt field from the test before typing (keyboard focus lands there after switching in normal use).
Blocker, attempted alternatives and next action (if blocked): not blocked
