# AC-12 — Two simultaneous ChatGPT subscriptions
Status: blocked
Tested implementation commit: 83ed5b6
Verification date and verifier: 2026-09-24/25, implementing agent (Claude Code)
OS / architecture / VS Code / harness versions: macOS 26.6.2 (25G83) arm64; VS Code 1.139.0 (stable, isolated --user-data-dir/--extensions-dir, VSIX installed with `code --install-extension`); Rust 1.89.0; Node 24.20.0; Git 2.50.1 (Apple); codex-cli 0.155.0-alpha.16.4 (ChatGPT.app bundle); Claude Code 2.1.246; OpenCode 1.15.13; overseerd 0.1.0 (protocol 1, parser 2026-09-24.1)
Harness, provider and redacted account identities (if applicable): not applicable (fixture harnesses; no accounts)
Prerequisites and fixture: Real Git repositories created per test under /tmp; isolated OVERSEER_HOME; isolated VS Code profile for UI scenarios

Steps or exact reproducible commands:

—

Expected result: Two distinct paid ChatGPT profiles run Codex tasks concurrently in separate worktrees with overlapping timestamps and distinct identities.

Actual result: Not run. Only one ChatGPT login is available to the agent (the shared `~/.codex`); a second isolated profile is not signed in. Concurrent separate-worktree execution itself is covered by AC-21 fixtures and the four concurrent runs in AC-35.

Evidence paths (test logs, redacted transcripts, screenshots/recording): AC-02 record
Live vs fixture coverage: None.
Known limitations and remaining platform/account combinations: macOS only; Linux belongs to AC-41.
Blocker, attempted alternatives and next action (if blocked): Owner signs in two isolated Codex profiles in Overseer (Accounts → Add Account Profile → Sign In, one per OpenAI account). Next: launch two tiny tasks concurrently and record `profile.status` identity fingerprints, overlapping run timestamps and both edits.
