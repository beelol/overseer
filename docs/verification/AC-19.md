# AC-19 — Actual native children
Status: verified
Tested implementation commit: fdf1340 (Claude), e055167 (Codex app-server grandchild, OpenCode live delegation)
Verification date and verifier: 2026-09-24/25, implementing agent (Claude Code)
OS / architecture / VS Code / harness versions: macOS 26.6.2 (25G83) arm64; VS Code 1.139.0 (stable, isolated --user-data-dir/--extensions-dir, VSIX installed with `code --install-extension`); Rust 1.89.0; Node 24.20.0; Git 2.50.1 (Apple); codex-cli 0.155.0-alpha.16.4 (ChatGPT.app bundle); Claude Code 2.1.246; OpenCode 1.15.13; overseerd 0.1.0 (protocol 1, parser 2026-09-24.1)
Harness, provider and redacted account identities (if applicable): Codex 0.155 (owner's ChatGPT login); Claude Code 2.1.246 (owner's claude.ai login, haiku); OpenCode 1.15.13 with a real local model (Ollama qwen3-coder:30b)
Prerequisites and fixture: Real Git repositories created per test under /tmp; isolated OVERSEER_HOME; isolated VS Code profile for UI scenarios

Steps or exact reproducible commands:

1. **Claude Code**: [claude-live](evidence/ui/claude-live/) — the prompt asks for an Agent subagent that itself launches an Agent; Overseer shows root → child → grandchild with exact provenance (Agent `tool_use` ids, `parent_tool_use_id` nesting, `system/task_*`), each child's output and completed status, all sharing the parent's workspace.
2. **Codex**: the exec transport captured a live depth-1 child ([codex-live](evidence/ui/codex-live/)). With the app-server transport and `extra_args: ["-c", "agents.max_depth=2"]`, a live run produced root → child → grandchild; each child thread's items and completion are attributed to that child ([log](evidence/ac-19/live-r-84d950aec2f6.log)). Default Codex depth is 1 (the first probe without the setting produced no grandchild).
3. **OpenCode**: a live run with a real local model (not the mock) delegated twice; the child and grandchild sessions were attached from OpenCode's session store ([log](evidence/ac-19/live-r-9db11694a476.log)); requires `agent.general.permission.task=allow` for a subagent to delegate further.
4. Fixture/regression coverage: `ac18_*`, `ac19_fixture_codex_live_transcript_children`, `ac19_fixture_codex_app_child_threads_nest_and_do_not_end_the_parent`.

Expected result: Live delegation sessions for Codex, Claude Code and OpenCode attached to the correct parent with output/status, plus a native grandchild where supported; unsupported depth documented.

Actual result: All pass. Depth limits: Codex default 1 (2 with `agents.max_depth=2`); Claude Code nests (depth 2 observed); OpenCode subagents need the `task` permission to delegate.

Evidence paths (test logs, redacted transcripts, screenshots/recording): `evidence/ui/claude-live/`, `evidence/ac-19/*.log`, `evidence/ui/codex-live/`
Live vs fixture coverage: Live for all three harnesses (OpenCode with a local model).
Known limitations and remaining platform/account combinations: macOS only; Linux belongs to AC-41.
Blocker, attempted alternatives and next action (if blocked): not blocked
