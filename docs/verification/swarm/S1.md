# S1 — Atlas tenant-isolation audit

Status: partial. Backend fixture revision: `d97695e`. Fixture: `fixtures/swarm/atlas-v1/manifest.json` (version 1). Support level: local Node.js 24, Express 5.2.1, PostgreSQL 16 and an in-process object-store emulator. No model worker, provider account, production service or VS Code instance is involved.

Input: the versioned Atlas backend seeds Alice as a member of workspace A, Bob in workspace B, a read-only and a revoked Alice token, two projects, two tasks and a foreign attachment. Each scripted J1–J7 test receives its own seeded PostgreSQL schema. The fixture source is pinned at the revision above; tests only mutate their own schemas.

Expected backend observations from the S1 specification: Alice's `PATCH /tasks/task-b-7` returns 200 and changes Bob's row from `Bob task` to `changed-by-alice`, where correct behavior would be 403 with no row change. Foreign `DELETE /tasks/:id` also removes a row. `GET /attachments/attachment-b-1/download` returns an object URL and the emulator serves Bob's content, a separate finding. `GET /projects/project-b-1` denies Alice even though its lookup starts by ID. Ordinary members cannot elevate roles; an admin in A can change an A membership but cannot change B. Export jobs bind to the authorized workspace and the worker query returns only that workspace's task. Read-only, revoked and former-member tokens cannot mutate. J7's independent schema must begin with Bob's original row and reproduce J2's task result without sharing its writes.

Observed: `./run-local.sh` passed seven Node tests against a disposable local PostgreSQL 16 container. The tests assert HTTP responses, before/after database rows, signed-object retrieval, queue row and query scope, token denial, and independent J2/J7 schema state. The source trace for the task flaw is `routes/tasks.ts` → `services/tasks.ts` → `repositories/tasks.ts::findById`; that repository query filters by task ID alone. The project route separately checks membership. The object-store emulator is process-local and serves only the fixture object. The temporary container was removed after the run.

Replay: from `fixtures/swarm/atlas-v1`, run `npm ci --ignore-scripts` followed by `./run-local.sh` with Node.js 24 and a local Docker daemon. `run-local.sh` binds PostgreSQL only to a random localhost port and removes its container on exit. `npm test` can instead use a supplied `ATLAS_DATABASE_URL` pointing to a disposable local database. No application fix or PR is created by these tests.

Remaining: no Swarm run has yet dispatched J1–J4 under the four-worker limit, routed declared account pools, handled D1/advisory/overlap and J4 applied revision, accepted J1 as a checked path, admitted J5 and J7, or produced the final evidence report. The missing-queue variant and all S1 fault variants remain unexecuted. There is no policy snapshot, plan/assignment revision trace, broker receipt trace, acceptance decision or complete artifact bundle. The backend test passes only the seeded service behavior; S1 and SWARM-64 stay incomplete.
