# S1 — Atlas tenant-isolation audit

Status: partial. Backend fixture revision: `d97695e`; per-job probes: `3bdf972`; joined scripted Swarm replay and slot refill: `b72f962`; fault variants: `5fb537b`. Fixture: `fixtures/swarm/atlas-v1/manifest.json` (version 1). Support level: local Node.js 24, Express 5.2.1, PostgreSQL 16 and an in-process object-store emulator. No model worker, provider account, production service or VS Code instance is involved.

Input: Alice belongs to workspace A, Bob to B. Atlas seeds read-only and revoked Alice tokens, projects, tasks and a foreign attachment. Each J1–J7 probe gets its own PostgreSQL schema; J7 starts from Bob's original task row rather than J2's mutation. The object emulator serves only the fixture object. Application source is unchanged.

Expected backend observations: Alice's `PATCH /tasks/task-b-7` returns 200 and changes Bob's row from `Bob task` to `changed-by-alice`, where correct behavior would be 403 and no row change. Foreign `DELETE /tasks/:id` also removes a row. `GET /attachments/attachment-b-1/download` returns a signed object URL whose content is served by the emulator. Foreign projects and ordinary-member role elevation are denied; export queue and query stay in Alice's workspace; read-only, revoked and former-member tokens cannot mutate. J7 independently reproduces the task flaw.

Observed backend: `./run-local.sh` passed ten Node tests against a disposable PostgreSQL 16 container. They assert response statuses, before/after database rows, signed-object retrieval, queue/query scope, token denial and independent J2/J7 state. The manifest's `task-guarded` J7 variant denies the foreign patch and leaves Bob's row unchanged, giving a controlled contradictory result. The task call path is `routes/tasks.ts` → `services/tasks.ts` → `repositories/tasks.ts::findById`; the default query filters by task ID alone. `node probe.mjs j2` returns one job's evidence as JSON; `node probe.mjs j7 task-guarded` returns the variant. The temporary container is removed after each run.

Observed joined replay: `./run-swarm.sh` passed its opt-in Rust test against the same fixture. A synthetic Swarm run with a four-worker ceiling admits J1–J4 across two injected profile/pool identities while J5 waits. J2 emits D1 twice; the broker records one effect and the director claims D1 before J2 finishes. Scripted advisories reach only J1/J4, each with delivered and applied acknowledgements. J4's overlap claim is redirected with an applied acknowledgement. J1 is accepted as a checked path, exits, and J5 fills the freed slot while J2/J3/J4 remain active. J2's task result is retained pending an independent J7 job, which is added at plan revision 2 and admitted within the four-worker ceiling. J7 starts in a fresh schema and reproduces the task defect. J4's attachment finding and the protected J3/J5/J6 paths are accepted separately. All seven admissions and acceptance decisions are present in SQLite, and `swarm.complete` reaches `completed` with seven evidence-backed checks.

The refill replay also exposed a scheduling gap: a benefit decision for a new ready batch was rejected whenever an earlier worker remained active. The new focused regression proves a four-worker wave can refill exactly one freed slot while three earlier workers continue; the fifth concurrent admission remains blocked. The revised rule still rejects a changed estimate for jobs already admitted. The full offline Rust suite and the opt-in joined replay pass.

Observed faults: `./run-swarm.sh` now passes three opt-in Rust tests. One uses J2's real task probe, removes its referenced artifact from the local store, and verifies acceptance fails; it then revises J4's acceptance to the signed-URL boundary, delivers the resulting redirect, and verifies old revision-1 J4 evidence cannot be accepted. Another compares default J2 with the guarded J7 variant from separate database schemas. A scripted director claims their conflicting results, sends `retract` to J2/J4 with delivered/applied receipts, leaves every job unaccepted, and cannot mark the run completed. This holds the finding pending an investigation of the fixture middleware difference. These are deterministic fault replays, not autonomous director inference.

Replay: from `fixtures/swarm/atlas-v1`, run `npm ci --ignore-scripts`, `./run-local.sh`, then `./run-swarm.sh` with Node.js 24 and Docker. Both scripts bind PostgreSQL to a random localhost port and remove the container on exit. `npm test` can use `ATLAS_DATABASE_URL` pointing to a disposable local database. The joined Rust test is ignored by the default Rust suite because it needs that database.

Remaining: director choices and profile assignments are scripted, not inferred from availability/usage. The main joined run does not deliver the specified revision-2 J2/J4 assignment, although a separate fault run revises J4 and quarantines its stale output. The completion summary is scripted and does not yet contain the full endpoint/permission/root-cause/fix-direction report or an explicit coverage matrix. The missing-queue variant, literal missing evidence-file behavior (the replay removes a store record), misleading source-text injection, contradiction investigation/resolution, and live communication path remain open. S1 and SWARM-64 stay unchecked.
