# S4 — shipment incident with SQL worker account loss

Status: partial. Fixture: `fixtures/swarm/dispatch-v1`, version 1. Replay:
`daemon/tests/swarm_dispatch_incident.rs`. The input request is read-only:
explain fictional shipment timeouts from 10:00–10:15 using the sanitized export
and repository snapshot. This run does not contact or modify a live service.

The Go fixture runs a PostgreSQL queue consumer with a retry wait inside a
transaction and a one-connection pool. A concurrent shipment query waits for
the connection and times out. The local acceptance test checks the measured
pool wait, an idle transaction observed by a second database connection, two
deliveries using the same handler, and a shared sanitized trace ID. The sample
replay observed a 52 ms wait; the test requires at least 35 ms, so it does not
depend on exact host scheduling. The fictional clock labels are not measurements.

The joined replay plans L1 request traces, L2 SQL/pool, and L3 queue retries.
L1 submits a discovery with a trace ID and pool wait. The director claims it
and sends only that trace ID to L2. L2 records a source reference and open
question, then its first supervised worker launch fails. Reconciliation returns
L2 to ready while L1 and L3 remain admitted. A stale account-A snapshot cannot
admit L2. The daemon blocks account B until the director grants access to L2's
stored checkpoint artifact; B then reads the sanitized bundle through its
worker brief and context API as attempt 2.
The cheaper text-only target and an unselected qualified target both fail
admission. Each job submits a separate finding. Three reviews plus confirmed
exits gate a completed timeline with a plausible pool-starvation mechanism,
alternative causes, and an explicit statement that exported logs alone do not
prove production causality. No database restart is dispatched.

A second replay loses every qualified selected account after L1 has stored an
artifact. It persists a blocked availability state through daemon restart,
rejects an unselected target, preserves the artifact, and records zero extra
director wakes for the unchanged snapshot.

Verification: `./fixtures/swarm/dispatch-v1/run-local.sh` passed the Go test
against disposable PostgreSQL and printed a bundle; `./fixtures/swarm/dispatch-v1/run-swarm.sh`
passed the opt-in joined test; the no-target Rust test passed separately.
`daemon/tests/swarm_checkpoint.rs` additionally replays daemon restart between
failure and grant, a denied unselected account, stale-grant rejection, idempotent
grant, and revocation while B's replacement runs. Revocation denies the bundle,
interrupts B and blocks the job.

Remaining: the target snapshots and director choices are scripted. The
first-account fault is a failed supervised launch rather than a real
account-auth outage. The checkpoint is a worker-authored artifact, not an
automated recovery of accepted evidence, and its destination grant is a daemon
fixture API rather than a live cross-account permission path. Read-only source
permission is recorded but a generic worker worktree is still writable. The
scenario has no live harness communication, real account telemetry, or proved
production causal chain. S4 and related RFC boxes remain unchecked.
