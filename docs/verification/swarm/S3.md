# S3 — Catalog cursor migration replay

Status: partial. Fixture: `fixtures/swarm/catalog-v1/manifest.json` (version 1). Support level: local Node.js 24 TypeScript REST fixture and scripted Swarm daemon APIs; no live model, provider account, external service, or VS Code launch.

Input: 24 independent resource routes start with page/offset pagination and the documented `{data, meta:{hasMore,limit,nextCursor}}` response. Seed rows have three equal `createdAt` values and a newer row is inserted between page fetches. The contract job first offers a cursor containing only `createdAt`, then repairs it to `(createdAt,id)` within its two-attempt limit. One module submits a revision-1 result before repair.

Expected: the offset baseline duplicates or shifts rows after insertion; the first cursor contract skips a tied row; the corrected contract visits every pre-existing row once while preserving the response shape and rejecting malformed cursor/limit and old page parameters. A stale module result must be redirected and unable to enter integration. Accepted patch artifacts must integrate only in an Overseer-owned worktree, and a combined check must fail before all modules are migrated and pass afterward. The source checkout must remain unchanged.

Actual: `node fixtures/swarm/catalog-v1/replay-backend.mjs` returned a three-stage trace: offset baseline failed, timestamp-only cursor failed, `(createdAt,id)` cursor passed all 24 routes and request edge checks. `cargo test --offline -p overseerd --test swarm_scenarios -- --ignored` passed with Node v24.20.0. Its scripted director created 25 logical jobs, submitted a stale `accounts` result, revised all 25 jobs, observed one redirect and rejection of the stale result, repaired the contract as attempt 2, then integrated the contract and 24 module patches as 26 commits in an isolated worktree. The combined checker failed after four module integrations and passed at the final commit. The pinned source checkout fingerprint was unchanged. This focused test is marked ignored in the default Rust suite because it requires Node 24 and permission to bind a local socket; run it explicitly with the command above.

Evidence: `fixtures/swarm/catalog-v1/`, `daemon/tests/swarm_scenarios.rs` (`catalog_s3_twenty_four_patches_need_a_combined_cursor_check`). The fixture uses in-memory rows and an ephemeral localhost HTTP server; no production data or paid model is involved.

Remaining: workers were represented by scripted attempts and patch artifacts, not 24 qualified live harness sessions. The test does not prove adaptive four-to-eight worker admission, backlog pressure, applied revision acknowledgements, director-led patch conflict repair, a shared dependency upgrade attempt, scope narrowing to 12 modules, actual combined build tooling, or final user-facing artifact presentation. S3 and its related SWARM criteria remain incomplete.
