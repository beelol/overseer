# S5 — Atlas under faults

Status: partial. Versioned backend: `fixtures/swarm/atlas-v1/manifest.json`, version 1. The S5 lost-result-receipt replay is `atlas_s5_lost_result_receipt_replays_once_after_restart` in `daemon/tests/swarm_atlas.rs`. Run `./fixtures/swarm/atlas-v1/run-swarm.sh` with Node.js 24 and Docker; the runner starts disposable PostgreSQL 16 and removes it afterward. The initial joined replay passed at `f9316c0`.

Input: a read-only audit of foreign task mutation. One planned J2 job claims the isolated `atlas-db-j2` writer resource at plan revision 1, with one selected fixture account and an injected exact `points` allowance. The daemon admits one worker. Its real Atlas probe observes `PATCH /tasks/task-b-7` returning 200, Bob's row changing from `Bob task` to `changed-by-alice`, and a reproduction artifact. The worker submits result message `atlas-s5-j2-result` with that artifact.

Fault and ordered trace: (1) the daemon durably records the result but the sender receives no reply; (2) the daemon is killed and restarted; (3) the worker retries the identical message ID and receives `duplicate: true`; (4) the director accepts the one artifact, a replay of that decision returns `duplicate: true`, and the worker exit is confirmed; (5) the director claims and applies the inbox batch; (6) evidence-gated completion produces `completed`. SQLite contains exactly one admission, artifact, result message and acceptance decision. The coverage row is `confirmed_application_defect`.

This proves the named S5 lost-receipt fault against the versioned backend. The original reply loss is injected by discarding the first response; it is not a live transport failure. Existing S1 replays cover stale/missing evidence, a contradictory guarded J7 result held unresolved, and a missing queue. Existing daemon fixtures cover some other mechanisms individually, but the remaining S5 faults have not been joined to Atlas with the required ordered traces. In particular director death between dispatch intent and acknowledgement, long-call redirect expiry, evidence-free heartbeat, shared-database contamination, invalid J8/J9/J99 plan, full storage, 2,000-message backpressure with Stop, revoked account identity, external allowance drop, last-result Stop, ceiling reduction and blocked deadline remain open. No SWARM-64 or S5 completion claim is made.
