# S5 — Atlas under faults

Status: partial. Versioned backend: `fixtures/swarm/atlas-v1/manifest.json`, version 1. The named S5 replays are `atlas_s5_lost_result_receipt_replays_once_after_restart` and `atlas_s5_director_death_recovers_one_dispatched_worker` in `daemon/tests/swarm_atlas.rs`. Run `./fixtures/swarm/atlas-v1/run-swarm.sh` with Node.js 24 and Docker; the runner starts disposable PostgreSQL 16 and removes it afterward. Six joined Atlas tests pass.

Input: a read-only audit of foreign task mutation. One planned J2 job claims the isolated `atlas-db-j2` writer resource at plan revision 1, with one selected fixture account and an injected exact `points` allowance. The daemon admits one worker. Its real Atlas probe observes `PATCH /tasks/task-b-7` returning 200, Bob's row changing from `Bob task` to `changed-by-alice`, and a reproduction artifact. The worker submits result message `atlas-s5-j2-result` with that artifact.

Fault and ordered trace: (1) the daemon durably records the result but the sender receives no reply; (2) the daemon is killed and restarted; (3) the worker retries the identical message ID and receives `duplicate: true`; (4) the director accepts the one artifact, a replay of that decision returns `duplicate: true`, and the worker exit is confirmed; (5) the director claims and applies the inbox batch; (6) evidence-gated completion produces `completed`. SQLite contains exactly one admission, artifact, result message and acceptance decision. The coverage row is `confirmed_application_defect`.

The director-death replay starts another J2 audit at plan revision 1. Its policy snapshot names one fixture target, one account and pool, an exact `points` window, a 100-milli-point estimate, and the `atlas-db-j2` exclusive writer claim. Ordered trace: (1) dispatch intent and one admission commit; the injected fault prevents launch acknowledgement, leaving no process; (2) confirmed director death advances generation 1→2, retaining the worker assignment and rejecting a generation-1 claim and revision; (3) daemon death/restart recovers the persisted intent and launches one supervised worker; (4) duplicate dispatch links to that same attempt/run; (5) the worker's real J2 probe sees a foreign PATCH return 200 and Bob's row change, then submits a reproduction artifact and result; (6) the replacement director reconciles terminal state, rejects an old-generation directive, claims the result, accepts its evidence, and completes the one-job run. SQLite records one admission and one linked worker launch. The final state is `completed`; the expected state is one retained worker with old-generation commands rejected. This is deterministic fixture recovery, not a live director model turn or live service failover.

These prove two named S5 faults against the versioned backend. The lost receipt is injected by discarding the first response; it is not a live transport failure. Existing S1 replays cover stale/missing evidence, a contradictory guarded J7 result held unresolved, and a missing queue. Existing daemon fixtures cover some other mechanisms individually, but the remaining S5 faults have not been joined to Atlas with the required ordered traces. In particular long-call redirect expiry, evidence-free heartbeat, shared-database contamination, invalid J8/J9/J99 plan, full storage, 2,000-message backpressure with Stop, revoked account identity, external allowance drop, last-result Stop, ceiling reduction and blocked deadline remain open. No SWARM-64 or S5 completion claim is made.
