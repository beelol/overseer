# Unverified Swarm acceptance criteria — dependency audit

Initially inspected at code revision `ffa08ba`; updated after fixture context and scheduling at `09d1a89`, writer isolation at `9a55173`, destination revocation at `51f072f`, cross-target grants at `9148e84`, revocation crash recovery at `f9b3018`, scripted control-event ordering at `a91579c`, Stop retry recovery at `59d59ff`, unconfirmed-exit readout at `f6886d0`, fixture patch integration at `14faa93`, integration crash reconciliation at `fca22c0`, combined-check gating at `089df35`, audit source-change admission at `eec9894`, joined Catalog scenario replay at `d48c33a`, verifier lease recovery at `ccb54c0`, terminal receipt replay and duplicate-flood Stop at `a0ef330`, prompt suspect retry at `be1c885`, bounded broker rejection at `66238db`, versioned Atlas backend evidence at `d97695e`, per-job probes at `3bdf972`, joined Atlas/slot-refill replay at `b72f962`, S1 stale/contradiction fault replay at `5fb537b`, the missing-queue variant at `9696f0b`, the joined LedgerPay S2 replay at `2fecb59`, and the persistent lost-ack/database replay at `de0959b`. This is a gap audit, not verification evidence. The ledger currently has 11 `unverified` and 53 `partial` criteria, each with a specific remaining gap. None is checked in the RFC; S1/S2/S3 are partial and S0/S4/S5 remain unverified.

The dependency clusters are:

1. Normal launch and Auto Mode integration: SWARM-01/02/05/13/15/16/17/25/26/28/51/59/63. Target and quota fixtures cannot establish live account availability, worker launch, descendant control, native usage or comparative benefit. Swarm admission counts ordinary active runs, but ordinary manual launches take priority and are not rejected by a Swarm-specific ceiling; a shared app-level limit and account-quota reservation remain missing. The separate Auto Mode implementation is in flight; the contract in `docs/rfcs/swarm-auto-contract.md` must be agreed before sharing account state.
2. Shared workspace and side effects: SWARM-18/19/35/52/57/58. Swarm fixture workers receive isolated worktrees and preserve a dirty source checkout. Accepted fixture patches integrate in a separate worktree; conflicts retain both evidence artifacts and do not corrupt the first commit. A durable intent reconciles fixture interruptions around Git commit and rejects unexpected workspace edits. The versioned S3 Catalog fixture now integrates a repaired contract and 24 module patches; a combined backend check fails after four modules and passes at the final commit. Dependents and final completion wait for patch integration. A daemon-crash replay holds an orphaned combined checker until it exits, marks its unacknowledged result interrupted and permits a fresh check. New runs default to no source-change permission; only an explicit isolated-write grant lets the fixture integration API apply patches, and migrated runs have no implicit grant. Fixture grants transfer accepted dependency context between allowed targets; revocation stops that delivery and a linked local worker. Hung-orphan termination, director conflict resolution, read-only worker sandboxing, live service mutation enforcement, live cross-target ACLs and side-effect reconciliation remain open.
3. Product presentation and truthful outcomes: SWARM-23/37/55. The extension has no Swarm view. The daemon fixture coverage readout distinguishes negative results, environment failures and confirmed defects. Atlas and LedgerPay now back missing-queue results with actual local service failures, but the final UI report remains unverified.
4. End-to-end scenarios: SWARM-64. S3 has a versioned 24-module backend and one scripted run joining contract repair, four-to-eight admission, review backpressure, 26 isolated integration commits and evidence-gated completion. Atlas S1 has a versioned Express/PostgreSQL backend and a joined scripted run with D1, overlap, seven isolated probes, slot refill, independent J7 and evidence-gated completion. Separate Atlas fault runs reject missing/stale evidence, hold contradictory J7 output with scripted retractions, and classify a missing export queue as blocked coverage. LedgerPay S2 has a versioned FastAPI/PostgreSQL/Redis backend and joined four-job replay with K2→director→K3 communication, a duplicate-grant reproduction, protected counterexample and missing-Redis blocked coverage. Its additional lost-ack replay persists a real database/queue across daemon restart, ties an external-state probe to the effect journal, and refuses blind redelivery. None proves an autonomous director choice or live provider path. Scripted context and round-robin admission are partial SWARM-34/36 evidence. Remaining S1/S2/S3 faults and detailed final artifacts, S0/S4/S5 and live harness lifecycle qualification have not been replayed.

Each `blocker` field in `coverage.json` names missing behavior for the 64 criteria and six scenarios. A partial criterion stays partial where fixture evidence exists; an unverified criterion remains unverified even if adjacent code has tests. No lack of live authorization was turned into a passing fixture claim.
